```markdown
# Campus Shuttle Management System

Campus Shuttle Management System — a small React + Vite app for booking and tracking campus shuttles (UI template adapted from various sources).

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.

## About

This project is a front-end prototype for a campus shuttle booking and monitoring system. It is intended as a starting point for development and demo purposes.
```
